## MODIFIED Requirements

### Requirement: 结算发起幂等补建

支付回调快路径（发现订单已处于已支付状态）MUST 幂等地补建结算 Saga：以订单标识构造确定性编排标识，重复发起时若该标识的 Saga 已存在则视为成功，不产生重复分支执行或重复副作用；结算发起失败时 MUST 返回错误使支付回调可被支付网关重试驱动补建。补建的系统性保证由结算待办发件箱承接（见"结算待办必达投递"）：支付网关重试仅为机会性保证，outbox relay 为必达保证。归档变更 harden-payment-settlement-chain design 决策 1 附注声明之"PAID 成功但建 saga 失败且 Stripe 不重试时无自动自愈、人工承接"残余风险（design.md:52-53）由本能力中的"结算待办必达投递"正式废除；"tombstone 纯人工承接"（design.md:71）由"定时对账扫描自愈"正式废除。

#### Scenario: 发布中断后经快路径补建

- **WHEN** 支付单已更新为已支付但结算 Saga 发起失败，随后支付回调重试到达并发现订单已支付
- **THEN** 快路径幂等补建结算 Saga，扣库存与用券副作用最终完成

#### Scenario: 首次发起即失败可被网关重试驱动

- **WHEN** 支付回调首次尝试发起结算 Saga 即失败（如编排服务不可达）
- **THEN** 发起返回错误，支付回调以失败响应结束使支付网关可重试，且不产生任何结算副作用

#### Scenario: 重复发起不产生重复结算

- **WHEN** 同一订单的结算 Saga 已存在（无论进行中或已完成）时再次发起
- **THEN** 发起被幂等接受（视为已存在），不产生第二套分支执行，不出现双扣库存或重复用券

## ADDED Requirements

### Requirement: 结算待办必达投递（Outbox）

支付单状态翻转为已支付与结算待办写入 MUST 在同一数据库事务内完成：待办行（status=pending）随状态翻转原子落库，使"翻转了但未触发编排"只可能存在于待办行挂起状态。结算服务 MUST 运行发件箱接力循环（relay）：以并发安全的方式批量锁定并取回到期 pending 待办（确保同一条待办不被多个 relay 实例并发处理，具体锁机制属实现），调用结算发起幂等入口（EnsureSaga），提交成功即置 done——done 语义为"已提交给编排器"，MUST NOT 等待或依赖 Saga 终态。接力失败 MUST 按指数退避重试并累加 attempts；超过上限后 MUST 置 dead 并输出需人工介入级结构化告警。relay 自身崩溃或重启后 MUST 能从落库状态续扫，不丢失、不重复触发副作用。

#### Scenario: 状态翻转与待办原子落库

- **WHEN** 支付回调将支付单翻转为已支付
- **THEN** 结算待办行（pending）与支付单状态在同一事务内落库，二者要么同时存在要么同时不存在

#### Scenario: relay 接力待办触发结算

- **WHEN** 一条 pending 待办到期且 relay 循环运行
- **THEN** relay 调用 EnsureSaga（幂等），提交成功后该待办被置 done，不等待 Saga 分支终态

#### Scenario: relay 崩溃后续扫不丢失

- **WHEN** relay 进程在处理待办期间崩溃或重启
- **THEN** 未置 done 的 pending 待办仍被后续 relay 循环接续处理，不产生重复副作用（EnsureSaga 幂等收敛）

#### Scenario: 重试超限转 dead 并告警

- **WHEN** 一条待办的累计尝试次数超过上限（如编排服务持续不可达）
- **THEN** 该待办被置 dead，系统输出包含订单标识与原因的需人工介入级结构化告警

### Requirement: 定时对账扫描自愈

结算系统 MUST 运行定时对账扫描，发现并分流"无信号的状态漂移"，扫描条件 MUST 带时间差阈值以给正常链路留处理窗口（避免误扫在途单）：

- **scan1（钱到了货没跟上）**：支付单已支付且 `paid_at` 早于阈值（默认 10 分钟）而订单未处于已支付时：订单仍待支付 → 幂等补建结算；订单已关闭/已取消 → 不得补结算（补偿语义正确），MUST 产出人工待办（退款另立项）。补建前 MUST 查询既有 Saga 状态：进行中 → 干等记录；已失败（已补偿墓碑）→ 以递增后缀编排标识（`settle:{orderId}:r{n}`，n 上限 3）有限自动重试，仍失败转人工告警。
- **scan2（该关没关）**：订单创建时间早于阈值（默认 35 分钟，大于订单超时关闭延迟 30 分钟）仍处待支付时 → 复用既有关单业务（行锁 + 状态机前置校验）自动关闭，其与迟到支付的竞态由既有状态机语义收敛（关单后钱到账 → 转入 scan1 的关闭+已支付分支产出人工待办）。
- 同一订单被扫描器、relay、对账重复发现时告警 MUST 聚合去重（订单维度）。

#### Scenario: 孤儿单自动补结算

- **WHEN** 支付单已支付超过 10 分钟而订单仍处于待支付，且不存在进行中的结算 Saga
- **THEN** 扫描器幂等补建结算 Saga（无既有 Saga 记录时用原始编排标识），扣库存与用券副作用最终完成

#### Scenario: 进行中 Saga 干等不干预

- **WHEN** 扫描器发现支付单已支付、订单待支付且既有结算 Saga 处于进行中（编排层仍在重试分支）
- **THEN** 扫描器不重复发起、不干预编排，仅记录观察（Saga 由编排层保证最终完成）

#### Scenario: 墓碑单有限自动重试

- **WHEN** 扫描器发现支付单已支付、订单待支付且既有结算 Saga 状态为已失败（已补偿）
- **THEN** 扫描器以递增后缀编排标识发起新结算 Saga（r2、r3，上限 3 次），成功则结算完成；超过上限仍失败则输出需人工介入级告警并产出待办

#### Scenario: 关闭订单的已支付款转人工产出

- **WHEN** 扫描器发现支付单已支付而订单已关闭/已取消（补偿已回滚，钱需退）
- **THEN** 不补结算，产出退款性质的人工待办并告警

#### Scenario: 超时未支付订单自动关闭

- **WHEN** 订单创建超过 35 分钟仍处于待支付（延迟队列漏关）
- **THEN** 扫描器复用既有关单业务关闭订单（状态机前置校验防误关），释放预扣库存与优惠券；若与迟到支付竞态，由状态机语义收敛

#### Scenario: 扫描窗口防误扫

- **WHEN** 一笔支付刚完成尚未超过时间差阈值（正常处理窗口内）
- **THEN** 扫描器不将其判定为异常，不产生重复补结算或误告警

### Requirement: Stripe 双向对账

结算系统 MUST 支持与 Stripe 账本的双向日终对账（以 Checkout Sessions 拉取，按规范化交易号关联本地支付单）：

- **方向1（Stripe 已扣款、本地支付单未支付）**：即 webhook 永久丢失。MUST 以 Stripe 账本为权威自动重放支付成功处理逻辑（重放安全垫：支付状态机 + 结算发起幂等 + 发件箱 + 扫描），重放失败转人工告警；重放前若校验发现金额或币种不一致，MUST 停止自动重放并转入人工待办，禁止按差异金额继续结算。
- **方向2（本地支付单已支付、Stripe 无对应账）**：MUST 输出需人工介入级告警并冻结后续自动化处置（不自动回滚——此时结算副作用大概率已完成，自动回滚会放大伤害）。
- 对账 MUST 校验金额与币种一致；差额 MUST 产出结构化对账结果（含订单标识、方向、金额差异）。

#### Scenario: 丢单自动重放

- **WHEN** Stripe 存在已完成的扣款记录而本地对应支付单未支付
- **THEN** 系统以 Stripe 数据重放支付成功处理（状态机幂等保证不重复生效），结算最终完成；重放失败则告警转人工

#### Scenario: 本地有账 Stripe 无账冻结告警

- **WHEN** 本地支付单已支付而 Stripe 无对应扣款记录
- **THEN** 输出需人工介入级告警并产出待办，不自动回滚已发生的结算副作用

#### Scenario: 对账差额结构化产出

- **WHEN** 日终对账发现任一方向的差异
- **THEN** 差异以结构化结果记录（订单标识、方向、本地与 Stripe 金额），供人工处置与审计

### Requirement: 异常单人工产出

扫描器、relay、对账发现的不可自动收敛异常（关闭+已支付、对账方向2、重试超限、墓碑超限）MUST 产出结构化人工待办：同一订单的重复发现 MUST 聚合为同一条待办（去重、可更新状态），待办 MUST 可查询、可标记处置结果。人工待办的处置（退款等）不在本能力范围内（另立项），本能力只保证异常不静默丢失。

#### Scenario: 跨层重复发现聚合去重

- **WHEN** 同一订单先被 relay 置 dead 告警、又被扫描器发现、再被对账标记差异
- **THEN** 人工待办按订单维度聚合为一条（更新发现来源与次数），不产生重复待办

#### Scenario: 待办可查可关

- **WHEN** 人工处理一条异常待办
- **THEN** 待办可被查询（含订单标识、来源、原因、次数）并标记处置结果，处置后不再重复告警
