## 1. 死代码清理

- [x] 1.1 删除 `common/utils/tracing/` 整个包（`tracing.go`），并全仓库确认无 `InitJaeger`/`InitWithOtelCollector`/`tracing.StartSpan` 等引用残留。验证：`git grep -n "common/utils/tracing\|InitJaeger\|InitWithOtelCollector" -- '*.go'` 无输出（覆盖全仓库，与 spec Req6 验收口径一致）。✅ 2026-08-30 实测：git grep exit=1 无输出。
- [x] 1.2 删除 12 个服务（auths/users/product/order/checkout/payment/inventory/audit/coupons/system/activity/admin）dev yaml 的 `PrometheusExt` 配置块（grep 实证均为死配置：main.go 无消费）；其中除 users 外的 11 个连带删除 config.go 的 `PrometheusExt PrometheusExtConf` 字段与 `PrometheusExtConf` 结构体定义（users config.go 无字段）。carts 无任何 PrometheusExt，不在清理范围。**search 例外**：其 `PrometheusExt` 为活配置（stats HTTP server 承载 `/metrics` + `/stats/rag/*` 业务 API），保留不动。验证：`grep -rln "PrometheusExt" services/` 输出仅剩 search 相关文件。✅ 2026-08-30 实测：仅剩 search 4 个文件（etc/search.yaml、internal/config/config.go、svc/servicecontext.go、internal/stats/http.go）；孤儿注释与文件尾空行已一并清理。
- [x] 1.3 运行 `make build`（或 `go build ./...`）确认删除死代码后编译通过，无未定义引用。✅ 2026-08-30 实测：`make build` 全 15 服务构建成功；另以 go.work 逐模块 `go build ./...` 覆盖 common/dal/tools/rag 亦通过。

## 2. 服务配置统一（DevServer + 指标端口规则）

- [x] 2.1 核实 `services/gateway` 是否为 go-zero `rest` 服务且走同一 `devserver.StartAgent` 路径，记录其 HTTP 端口（8888）并确定 metrics 端口（9888，服从 +1000 规则）。search 已核实：服务端口 8081（`etc/search.yaml:2`），指标端口由活代码 stats HTTP server 提供，按 +1000 规则由 9113 调整为 9081（D1 例外，非 DevServer）。验证：将核实结论写入任务说明，作为 2.3/2.4 的依据。✅ 2026-08-30 核实结论：gateway 使用 go-zero `gateway` 包（非裸 `rest`），`GatewayConf` 内嵌 `rest.RestConf`（config.go:11），HTTP 端口 8888（etc/gateway.yaml:3）；DevServer 链路源码确认：`gateway.MustNewServer`（gateway.go:49）→ go-zero `gateway/server.go:48` `rest.MustNewServer(c.RestConf)` → `rest/server.go:50` `c.SetUp()` → `core/service/serviceconf.go:77` `devserver.StartAgent(sc.DevServer)`，与 zrpc 同路径，配置 DevServer 确定生效，metrics 端口定 9888。
- [x] 2.2 为以下 dev 配置补 `DevServer`（`Enabled: true, Host: 0.0.0.0, Port: 110xx, EnablePprof: false`），端口遵循"服务端口+1000"：`services/{users,product,carts,order,checkout,payment,inventory,coupons,system,activity,admin}/etc/*.yaml`（dev 版，11 个）；auths/audit dev 已有 DevServer（11000/11008），核对端口符合映射表即可。search dev 不补 DevServer（走 D1 例外，见 2.3）。验证：14 个 dev yaml 的 metrics 来源齐备（13 个 DevServer + search stats server）且端口符合映射表。✅ 2026-08-30 实测：11 个补齐 + auths/audit 核对通过，13 个 DevServer 端口 11000–11012 与映射表一一相等，各 dev yaml ListenOn 端口与 design 表一致。
- [x] 2.3 按 2.1 结论为 gateway dev 配置补 `DevServer`（Port 9888）；将 `services/search/etc/search.yaml` 的 `PrometheusExt.Port` 从 9113 调整为 9081。system/activity/admin/search/gateway 无 prod 配置文件，本 change 不创建（prod 侧随线上部署独立 change）。验证：gateway dev yaml 含 DevServer(9888)；search.yaml 端口为 9081。✅ 2026-08-30 实测：gateway.yaml DevServer(9888, EnablePprof:false) 已加；search.yaml PrometheusExt.Port=9081，未引入 DevServer（D1 例外）。
- [x] 2.4 校验所有含 `DevServer` 的配置（dev 与既有 10 个 prod yaml）`EnablePprof: false`。验证：`grep -A5 "DevServer:" services/*/etc/*.yaml services/*/etc/*.prod.yaml` 输出中无 `EnablePprof: true`。✅ 2026-08-30 实测：dev 14 项 + prod 10 项全部 `EnablePprof: false`，0 处 true。

## 3. Prometheus 抓取配置重写

- [x] 3.1 按 design.md 端口映射表重写 `construct/observability/conf/prometheus.yml` 全部 `static_configs`：target 端口对齐 110xx（gateway 9888、search 9081）；宿主机服务 target 的 host 由 `localhost` 改为 `host.docker.internal`（Prometheus 在 bridge 网络容器内，容器内 localhost 抓不到宿主机服务；Prometheus 自抓 9090 保留）。验证：prometheus.yml 中每个 job 的 target 端口与对应服务 yaml 的指标端口一一相等，且宿主机服务 target 均为 `host.docker.internal`。✅ 2026-08-30 实测：16 个 job 逐一比对（脚本断言）全部相等；仅 prometheus 自抓保留 localhost:9090。
- [x] 3.2 校验 prometheus.yml 的 `service`/`namespace` 标签与服务名对齐（现有 namespace=go-mall 保留），并决定是否补 `env` 标签与 promtail 侧 `env: development` 标签口径一致。验证：yaml 语法通过 `docker compose -f construct/observability/docker-compose.yaml config` 校验。✅ 2026-08-30 决策与实测：service 标签由 `*-rpc` 改为与服务名/job 名一致（order、users 等 15 个）；补 `env: development` 标签与 promtail 口径对齐；compose config 校验见 3.2 执行记录（docker compose config 通过）。

## 4. 本地一键启动补全观测栈

- [x] 4.1 在 `scripts/start-unified.sh` 中新增 `OBSERVABILITY_STACK="${PROJECT_ROOT}/construct/observability/docker-compose.yaml"`，并在起 LOKI_STACK 后追加 `docker compose -f "$OBSERVABILITY_STACK" up -d jaeger prometheus`（含健康检查等待，参考现有 `wait_for_container_health`）。验证：脚本启动后 `docker ps` 含 jaeger + prometheus 容器且状态 healthy/running。✅ 2026-09-01 实测（脚本路径）：start 输出 "启动 Jaeger/Prometheus... Jaeger/Prometheus 就绪 (jaeger ui:16686 prometheus:9090)"；docker ps：observability-jaeger-1 healthy、observability-prometheus-1 running。
- [x] 4.2 在 `scripts/start-unified.sh` 的 stop 分支追加 `docker compose -f "$OBSERVABILITY_STACK" down`。验证：执行 stop 后 jaeger/prometheus 容器均已退出。✅ 2026-09-01 实测：stop 后 `docker ps -a` 无任何 go-mall/loki/promtail/grafana/jaeger/prometheus 容器（依赖栈、日志栈、观测栈三栈全部退出，含新增的 OBSERVABILITY_STACK down）。
- [x] 4.3 在 `scripts/start-unified.sh` 的 status 分支追加 observability 栈状态展示（参照现有 LOKI/依赖栈展示模式）。验证：`./scripts/start-unified.sh status` 输出含 jaeger/prometheus 行。✅ 2026-08-31 实测：status 输出新增 "Observability stack:" 段，含 `[up/down] jaeger port=16686 container=observability-jaeger-1` 与 prometheus 行，Access URLs 补 jaeger/prometheus 条目。
- [x] 4.4 在 `scripts/start-unified.sh` 启动服务前，利用现有 `port_in_use` 函数对 110xx 指标端口（及 9888/9081）做占用预检，冲突时报错退出并提示占用进程。验证：人为占用 11004 后启动脚本输出明确报错信息。✅ 2026-08-31 实测：Python 监听 11004 后执行 start，脚本在服务启动前输出"指标端口已被占用: 11004（服务端口+1000 规则，请释放后重试）"并附 lsof 占用进程明细后退出（exit 1）。修复过程中发现 `$port（` 全角字符紧跟变量名会被 bash 并入变量名解析（unbound variable），已改为 `${port}` 花括号形式。

## 5. 端到端验证

- [x] 5.1 本地执行 `./scripts/start-unified.sh`，确认三栈 UI 可达：Grafana http://localhost:3001、Jaeger UI http://localhost:16686、Prometheus http://localhost:9090。验证：curl 各 URL 返回 200。备注：本地同时存在 observability 栈自带 Grafana(:3000)，未预配数据源，本 change 验收走 Prometheus(:9090)/Jaeger(:16686) 原生 UI，不依赖任一 Grafana。✅ 2026-09-01 实测：15 服务 + frontend + Loki/Grafana 栈由脚本拉起，Prometheus:9090=200、Jaeger:16686=200、Grafana:3001=200（跟随重定向）。〔为使一键启动可用，顺带修复两处预存缺口并披露：① launchd 拒载外置卷脚本（exit 126）→ start_supervised_process 增加回退 nohup 自愈；② ALL_SERVICES 遗漏 search 致 gateway 因 search.rpc 无注册而 panic → 补入清单；③ Stripe CLI 未登录原为硬退出 → 改为警告跳过（非致命）；④ bitnami/prometheus:latest 镜像已从 Docker Hub 消失 → 换 prom/prometheus:latest 并修正挂载路径 /etc/prometheus/〕
- [x] 5.2 启动全部服务后，`curl http://localhost:11004/metrics`（order）等抽验 3 个服务的 metrics 端点返回 Prometheus 文本指标。验证：返回 200 且含 `go_` / `process_` 等指标行。✅ 2026-09-01 实测：order:11004 / auths:11000 / gateway:9888 / search:9081 四端点均 HTTP 200，各含 42 行 go_/process_ 指标（超出抽验 3 个的要求）。
- [x] 5.3 打开 Prometheus UI → Status → Targets，确认所有 job 的 target 状态为 UP（无 connection refused）。验证：每个 endpoint State=UP。✅ 2026-09-01 实测：Prometheus API /api/v1/targets 返回 16/16 全 UP，0 down（含 gateway 9888、search 9081、13 个 110xx 与 prometheus 自抓）。
- [x] 5.4 触发一次跨服务调用（如 gateway→checkout→order），在 Jaeger UI 按入口服务检索到完整 trace；在 Grafana/Loki 以 `{service="order"}` 检索到日志。另抽验采样率配置生效：将任一服务 dev 采样率临时设为 0.1，确认其导出 trace 数量明显少于全量后恢复。验证：Jaeger 显示含下游 span 的 trace、Loki 返回 order 日志条目、低采样率下 trace 稀疏。✅ 2026-09-01 实测：① 订单链路完整 trace `0b6cc15d...`：18 spans 横跨 gateway/order.rpc/checkout.rpc/users.rpc 四服务（HTTP /api/v1/orders → order.CreateOrder → checkout.GetCheckoutDetail/UpdateStatus2Order → users.GetAddress → sql），GetOrder 链 6 spans 横跨 gateway+order.rpc；② Loki `{service="order"}` 返回日志流（labels env=development, filename=/var/log/go-mall/order.log，样本含 CreateOrder 条目）；③ 采样率抽验：gateway Sampler 临时 0.1 后 30 次调用仅导出 3 条 trace（≈10%），已恢复 1.0（注：go-zero 采用 `ParentBased(TraceIDRatioBased)`，采样率在 trace 根——即 gateway——处生效，下游继承根决策，故在 gateway 上验证）。〔apply 揭示并修正三处 trace 接线缺陷（design D2 已同步更正）：11 个 dev yaml 的 `Batcher: zipkin` 指向 14268（Jaeger 原生端点，zipkin JSON 被 400 拒收）→ 统一改为 `http://localhost:9411/api/v2/spans`；product/system/activity/search 无 Telemetry 块 → 补齐（gateway 复核确认原有块，15 服务全部齐备）；jaeger all-in-one 1.61 未启用 zipkin 收集器（"Not listening for Zipkin HTTP traffic"）→ compose 补 `COLLECTOR_ZIPKIN_HOST_PORT=:9411`。k8s/prod 同类错配随线上部署独立 change 修正〕
- [x] 5.5 运行 `make lint` 与 `make test-unit` 确认无回归。验证：lint 通过、单元测试通过。✅ 2026-08-31 实测：make lint（go-ci-build.sh 全量检查）"所有检查通过"；make test-unit 37 包 94 用例全过（fail=0 skip=0，63s，报告 .artifacts/unit-report/summary.txt）。
- [x] 5.6 更新 `docs/logging-system.md` 与 `docs/jaeger/README.md`，标注两处 compose 现由 start-unified 统一拉起（手动方式保留作备选）。验证：文档路径与实际一致，无指向不存在的统一 compose。✅ 2026-08-31 实测：两文档均补充"两处独立 compose + start-unified 统一拉起"说明，明确 infrastructure=日志栈、construct/observability=Jaeger+Prometheus，手动方式保留为备选；未引用任何不存在的统一 compose。
